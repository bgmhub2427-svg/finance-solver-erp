// Daily report auto-save — disabled auto-download to avoid unexpected file downloads.
// Can be triggered manually from Settings page instead.
export function useDailyReportAutoSave() {
  // No-op: auto-download removed for better UX
}
